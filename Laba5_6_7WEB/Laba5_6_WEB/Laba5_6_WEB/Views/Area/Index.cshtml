@model IEnumerable<Laba5_6_WEB.Models.AreaVM>

@{

    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.ActionLink("Create", "MakeArea", new { id = item.Id })
            @Html.ActionLink("Update", "UpdateArea2", new { id = item.Id })
            @Html.ActionLink("Delete", "DeleteArea", new { id = item.Id })
        </td>
    </tr>
    }

</table>
