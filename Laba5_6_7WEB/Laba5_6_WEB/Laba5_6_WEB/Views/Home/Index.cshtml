@model IEnumerable<Laba5_6_WEB.Models.HeroVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Create", "MakeHero", new { id = item.Id })
            @Html.ActionLink("Update", "UpdateHero2", new { id = item.Id })
            @Html.ActionLink("Delete", "DeleteHero", new { id = item.Id })
        </td>
    </tr>
    }

</table>